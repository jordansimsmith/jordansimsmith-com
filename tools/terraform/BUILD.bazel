load("@rules_python//python:defs.bzl", "py_binary")

py_binary(
    name = "apply",
    srcs = ["apply.py"],
    args = ["$(rootpath //:MODULE.bazel)"],
    data = ["//:MODULE.bazel"],
    visibility = [
        "//:__pkg__",
    ],
)
