[project]
name = "tcg-scan-orb-rerank-spike"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "opencv-python-headless",
    "pillow",
]

[dependency-groups]
dev = ["pytest"]
